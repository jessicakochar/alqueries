[project]
name = "alqueries"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "==3.11.12"
dependencies = ["numpy>=2.4.4", "scikit-learn>=1.8.0", "torch>=2.11.0"]

[tool.uv]
package = true

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
